apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-service
  namespace: prod
  labels:
    app: dashboard-service
    environment: prod
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: dashboard-service
  template:
    metadata:
      labels:
        app: dashboard-service
    spec:
      containers:
        - name: dashboard-service
          image: circleguard/dashboard-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-service:5432/circleguard_dashboard"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: pg-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-credentials
                  key: password
            - name: PROMOTION_SERVICE_URL
              value: "http://promotion-service:8088"
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"

apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: prod
spec:
  selector:
    app: dashboard-service
  ports:
    - port: 8084
      targetPort: 8084